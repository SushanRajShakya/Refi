import React from 'react';

import Spinner from '../shared/spinner';

// TODO: Display a loader here while verification goes on.
const AuthorizationUI = () => {
  return <Spinner isLoading={true} />;
};

export default AuthorizationUI;
