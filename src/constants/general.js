export const textTransform = {
  UPPERCASE: 'uppercase',
  CAPITALIZE: 'capitalize',
  NONE: 'none',
  LOWERCASE: 'lowercase',
};

export const applicantType = {
  PRIMARY: 'PRIMARY',
};

export const DROPDOWN_OPTIONS_LIMIT = 10000;

export const isUserValid = {
  INVALID_USER: '-1',
  DEFAULT_USER: '0',
};

export const DEFAULT_USER_ID = '$$$$';

export const WINDOW_SIZE_DESKTOP = 768;
